[pytest]
addopts = --strict-markers --html=reports/selenium-report.html --self-contained-html -ra
testpaths = tests
pythonpath = .
markers =
    smoke: critical browser checks
    regression: broad UI regression checks
    negative: negative validation scenarios
